{
  "name": "grpctestify-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action to install and run grpctestify",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/main.js && node --check dist/main.js",
    "all": "npm run build && npm run check"
  },
  "engines": {
    "node": ">=24"
  }
}
